query pullRequestFilesList($id: Int!) {
  pullRequest(id: $id) {
    id
    files {
      id
      stats {
        added
        deleted
        binary
      }
      operation
      oldName
      name
      type
    }
  }
}
