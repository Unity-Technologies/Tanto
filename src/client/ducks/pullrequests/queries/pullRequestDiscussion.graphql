query pullRequestDiscussion($id: Int!){
  pullRequest(id: $id) {
    id
    comments {
      id
      text
      created
      status
      author {
        id
        username
        fullName
      }
      issue {
        id
        status
      }
    }
  }
}

