query latestRepositoryBuilds($repository: String!, $revision: String){
  sourceStamp(repository: $repository, revision: $revision) {
    id
    revision
    latestBuilds {
      total
      nodes {
        id
        number
        result
        finish
        start
        buildTime
        builder {
          friendlyName
          name
          project
        }
      }
    }
  }
}
